# HIGHER ORDER FUNCTIONS

`.filter`

### Filters out the value that is specified and returns it

```javascript
array.filter((x) => "Dog");
```

`.map`

```javascript
array.transform((x) => x + "dog");
```

`.reduce`
